<!DOCTYPE html>
<html>

  <head>
    <link href="https://fonts.googleapis.com/css2?family=Yomogi&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag 'devise/registration' %>
  </head>

  <body>

    <%= render "components/homebar" %>

    <div class="sign-up">

      <div class="background">

        <div class="contents">

          <div class="title">
            <h2>Sign up</h2>
          </div>

          <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class:"form"}) do |f| %>


            <%= render "components/error_messages", model: @user %>

            <div class="content">
              <p>名前</p>
              <%= f.text_field :name, autofocus: true, autocomplete: "name", class:"input", placeholder:"10字以内" %>
            </div>

            <div class="content">
              <p>メールアドレス</p>
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"input" %>
            </div>

            <div class="content">
              <p>パスワード</p>
              <%= f.password_field :password, autocomplete: "new-password", class:"input" %>
            </div>

            <div class="content">
              <p>パスワード(確認)</p>
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"input" %>
            </div>

            <div class="content">
              <%= f.submit "Sign up", class:"form-btn"%>
            </div>

          <% end %>

        </div>

      </div>

    </div>

    <%= render "components/footer_second" %>

  </body>

</html>

